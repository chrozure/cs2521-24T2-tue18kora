#include <stdbool.h>

struct edge {
    int v;
    int w;
};

#include "Graph.h"

bool isEulerPath(Graph g, struct edge e[], int nE);



bool isEulerPath(Graph g, struct edge e[], int nE) {
    return false;
}
